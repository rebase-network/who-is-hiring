[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "who-is-hiring"
version = "0.1.0"
description = "Issue-driven jobs board build pipeline"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
